spring:
  datasource:
    url: jdbc:mysql://localhost:3306/data_tool?useSSL=false&serverTimezone=UTC
    username: root
    password: better0416
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379
    password:
    database: 0
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
server:
  port: 8089