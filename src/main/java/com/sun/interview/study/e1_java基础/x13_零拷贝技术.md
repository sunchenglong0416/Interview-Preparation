##  传统IO
##  传统IO流程
![](/src/main/resources/images/io.png "传统IO流程")
##  过程分析
  结合着上图，我们再巩固一下：整个流程需要 4 次拷贝、4 次上下文切换。
  数据拷贝次数：2 次 DMA 拷贝，2 次 CPU 拷贝
  CPU 切换次数：4 次用户态和内核态的切换
  而实际的 IO 读写，有时候需要进行 IO 中断，同时也需要 CPU 响应中断，拷贝次数和切换次数比预期的还要多，以至于当客户端进行资源文件下载的时候，传输速度总是不尽人意。
##  零拷贝技术实现IO
##  mmap技术
##  流程
![](/src/main/resources/images/mmapio.png "mmap-IO流程")
##  过程分析
  从上图可以清晰的看到 mmap 内存映射拷贝过程中 数据拷贝的次数 和 状态切换的次数：
  数据拷贝次数：2 次 DMA 拷贝，1 次 CPU 拷贝
  CPU 切换次数：4 次用户态和内核态的切换
  整个过程省掉了数据在内核缓冲区和用户缓冲区之间的 CPU 拷贝环节，在实际的应用中，对资源的拷贝能提升不少性能
##  sendfile技术
  Linux 2.1 以及之后的内核版本中，引入了一个系统调用方法：sendfile()
  当调用 sendfile() 时，DMA 将 数据从 磁盘 拷贝 到 内核缓冲区，然后将 内核缓冲区 的数据 直接拷贝 到 右边的 socket 缓冲区中，最后 DMA 把数据 从 socket 缓冲区中 通过网卡传输给客户端
  流程
  ![](/src/main/resources/images/sendfile.png "sendfile-IO流程")
过程分析
从上图可以清晰的看到 sendfile 的整个过程中 数据拷贝的次数 和 状态切换的次数：
数据拷贝次数：2 次 DMA 拷贝，1 次 CPU 拷贝
CPU 切换次数：2 次用户态和内核态的切换
相比 mmap 内存映射方式，Linux 2.1 内核版本中 sendfile 拷贝流程省掉了 2 次状态切换
但是，这还没完。
在 Linux 2.4 内核版本中，对 sendfile 这个系统方法做了优化升级，引入 SG## DMA 技术，其实就是对 DMA 拷贝加入了 scatter/gather 操作，让DMA可以直接从内核空间缓冲区中将数据读取到网卡。需要注意的是 这个特性需要 DMA 控制器的支持。
使用这个特性来实现数据拷贝，可以把 CPU仅剩的那一次拷贝也省去，从而真正的实现整个过程中 CPU拷贝数据的次数为零。
sendfile with sg## dma技术
流程
  ![](/src/main/resources/images/sendfilesgdma.png "sendfile-IO流程")
过程分析
sendfile With SG## DMA  拷贝流程
数据拷贝次数：2 次 DMA 拷贝，0 次 CPU 拷贝
CPU 切换次数：2 次状态切换
可以发现，sendfile With SG## DMA 实现的拷贝，其中 2 次数据拷贝都是 DMA 拷贝，全程都没有通过 CPU 来拷贝数据，
就是操作系统真正意义上的        零拷贝（Zero## copy) 技术，相比其他拷贝方式，效率最佳。
##  Java中的零拷贝
##  Java中对零拷贝的实现是nio包中的 FileChannel 这个类
##  mmap和sendfile 在Java里也都是支持的，都是通过nio包里的FileChannle，就是文件通道来获取
  所以只要部署你代码的机器上安装的操作系统它底层支持零拷贝的话，那你Java代码写的零拷贝就能生效。
  Java中具体是用 FileChannle 的 transferTo 和 transferFrom 来调操作系统底层的sendFile方法。
##  Netty使用的零拷贝
  Netty 中的零拷贝和传统 Linux 的零拷贝技术的实现不太一样，Netty 中的零拷贝技术主要是通过优化用户态的操作来提升 IO 的执行速度，从而实现零拷贝的。
  PS：所有可以提升 IO 执行效率的操作或手段都可以称之为零拷贝技术。

  Netty 中的零拷贝技术主要有以下 5 种实现：
 * 1.使用堆外内存：避免 JVM 堆内存到堆外内存的数据拷贝，从而提升了 IO 的操作性能。
 * 2.使用 CompositeByteBuf 合并对象：可以组合多个 Buffer 对象合并成一个逻辑上的对象，避免通过传统内存拷贝的方式将几个 Buffer 合并成一个大的 Buffer。
 * 3.通过 Unpooled.wrappedBuffer 合并数据：可以将 byte 数组包装成 ByteBuf 对象，包装过程中不会产生内存拷贝。
 * 4.使用 ByteBuf.slice 共享对象：操作与 Unpooled.wrappedBuffer 相反，slice 操作可以将一个 ByteBuf 对象切分成多个 ByteBuf 对象，切分过程中不会产生内存拷贝，底层共享一个 byte 数组的存储空间。
 * 5.使用 FileRegion 实现零拷贝：FileRegion 底层封装了 FileChannel#transferTo() 方法，可以将文件缓冲区的数据直接传输到目标 Channel，避免内核缓冲区和用户态缓冲区之间的数据拷贝，这属于操作系统级别的零拷贝。
  它们的具体实现如下。
##  3.1 使用堆外内存
  正常情况下，JVM 需要将数据从 JVM 堆内存拷贝到堆外内存进行业务执行的，这是因为：
*  1.操作系统并不感知 JVM 的堆内存，而且 JVM 的内存布局与操作系统所分配的是不一样的，操作系统并不会按照 JVM 的行为来读写数据。
*  2.同一个对象的内存地址随着 JVM GC 的执行可能会随时发生变化，例如 JVM GC 的过程中会通过压缩来减少内存碎片，这就涉及对象移动的问题了。
  而 Netty 在进行 I/O 操作时都是使用的堆外内存，可以避免数据从 JVM 堆内存到堆外内存的拷贝。
*  3.2 使用CompositeByteBuf合并对象
  CompositeByteBuf 可以理解为一个虚拟的 Buffer 对象，它是由多个 ByteBuf 组合而成，但是在 CompositeByteBuf 内部保存着每个 ByteBuf 的引用关系，从逻辑上构成一个整体。使用 CompositeByteBuf 我们可以合并两个 ByteBuf 对象，从而避免两个对象合并时需要两次 CPU 拷贝操作的问题，在没有使用 CompositeByteBuf 时，我们的操作是这样的：
  代码语言：java
